<manifest package="de.fabianonline.geotweeter"
    android:versionCode="1"
    android:versionName="Wait, what?" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />
    
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".TimelineActivity"
            android:label="@string/title_activity_timeline" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".activities.NewTweetActivity"
            android:label="@string/title_activity_timeline" >
        </activity>
        
        <activity
            android:name=".GeneralPrefsActivity"
            android:label="@string/settings" >
        </activity>
        
        <activity
            android:name=".activities.SettingsAccounts"
            android:label="@string/settings_accounts_title">
            <intent-filter>
                <action android:name=".activities.SettingsAccounts" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
            
        <activity
            android:name=".AuthenticateAccount"
            android:label="@string/authenticate_account">
        </activity>        
    </application>

</manifest>